# Queries

Keep reusable KQL and WIQL queries here, grouped by domain.

Suggested layout:
- `afd-waf/` for Front Door WAF queries
- `sentinel/` for Sentinel analytics queries
- `aks-*.kql` for AKS policy and diagnostic checks
